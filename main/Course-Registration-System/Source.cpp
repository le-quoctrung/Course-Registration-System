
#include "Login_System.h"
#include "Console.h"
#include "time.h"
#include "Tokenizer.h"
#include "school.h"
#include "file.h"
#include "display.h"
#define xPos 307/42
#define yPos 863/53

int main()
{
	//ListClass* classes = new ListClass;
	//createEmptyList(classes);
	////add node class
	//addClass(classes, 1, "CLC");

	////read from list to node class
	//ReadListToClass("allStudent.csv", getNode(classes->head,1));
	//OutputListStudents(getNode(classes->head, 1));
	//system("pause");
	//CreateStudentAccounts(getNode(classes->head, 1));

	/*
	
	to do list:
	session
	update course function

	menu for staffs
	menu for students
	
	export csv file for grading
	import csv grading file



	*/

	return 0;

}

